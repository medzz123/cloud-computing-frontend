import { makeStyles } from '@material-ui/core';

export const useHomeStyles = makeStyles({
  cardContainer: {
    width: '100%',
  },
  centerSpinner: {
    display: 'flex',
    justifyContent: 'center',
  },
});
