import { environment } from 'src/environments/environment';

export const USER_URL = `${environment.webServerUrl}/user`;
export const EVENT_URL = `${environment.webServerUrl}/event`;
export const CREATE_USER_URL = `${environment.webServerUrl}/create-user`;
export const GET_REPLY_URL = `${environment.webServerUrl}/get-reply`;
export const REPLY_URL = `${environment.webServerUrl}/reply`;



